--- a/inc/multi_ap.h
+++ b/inc/multi_ap.h
@@ -666,6 +666,7 @@ unsigned short create_vs_info_for_wts_co
 unsigned short create_vendor_specific_wts_content_message(unsigned char *buf,
 	struct p1905_managerd_ctx *ctx);
 int delete_exist_steer_cntrl_db(struct p1905_managerd_ctx *ctx);
+void set_trx_config_for_bss(struct p1905_managerd_ctx*ctx, unsigned char map_vendor_extension, unsigned char *bss_mac);
 
 #endif /*MAP_H*/
 
--- a/src/_1905_lib_io.c
+++ b/src/_1905_lib_io.c
@@ -169,6 +169,7 @@ int _1905_set_wireless_setting(struct p1
 		priority = bss->priority + 1;
 		config = &ctx->current_autoconfig_info.config_data[i];
 		p = fill_bss_config(p, config, bss);
+		set_trx_config_for_bss(ctx, config->map_vendor_extension, bss->mac);
 		debug(DEBUG_OFF, "interface=%s priority=%d config_SSID=%s hidden_ssid=%d\n",
 			bss->ifname, bss->priority, config->Ssid, config->hidden_ssid);
 		i++;
@@ -188,6 +189,7 @@ int _1905_set_wireless_setting(struct p1
 			config = &ctx->current_autoconfig_info.config_data[j++];
 
 			p = fill_bss_config(p, config, bss);
+			set_trx_config_for_bss(ctx, config->map_vendor_extension, bss->mac);
 			debug(DEBUG_TRACE, "interface=%s priority=%d config_SSID=%s hidden_ssid=%d\n",
 				bss->ifname, bss->priority, config->Ssid, config->hidden_ssid);
 			if (j >= auto_config_num)
@@ -792,6 +794,7 @@ int _1905_update_bss_info_per_radio(stru
 			}
 		}
 		p = fill_bss_config(p, &config, bss);
+		set_trx_config_for_bss(ctx, config.map_vendor_extension, bss->mac);
 		debug(DEBUG_TRACE, "interface=%s priority=%d config_SSID=%s, hidden_ssid=%d\n",
 			bss->ifname, bss->priority, config.Ssid, config.hidden_ssid);
 		i++;
@@ -826,6 +829,7 @@ int _1905_update_bss_info_per_radio(stru
 			}
 			sum++;
 			p = fill_bss_config(p, &config, bss);
+			set_trx_config_for_bss(ctx, config.map_vendor_extension, bss->mac);
 			debug(DEBUG_TRACE, "interface=%s priority=%d config_SSID=%s, hidden_ssid=%d\n",
 				bss->ifname, bss->priority, config.Ssid, config.hidden_ssid);
 			if (sum >= auto_config_num)
--- a/src/cmdu.c
+++ b/src/cmdu.c
@@ -142,6 +142,18 @@ unsigned char cmdu_src_mac_check(struct
     return 0;
 }
 
+int cmdu_recv_intf_check(struct p1905_managerd_ctx *ctx)
+{
+	if (!(ctx->itf[ctx->recent_cmdu_rx_if_number].trx_config & (RX_MUL|RX_UNI))) {
+		debug(DEBUG_ERROR, "recv intf %s trx_config(%02x) has no receiving cap; drop!\n",
+			ctx->itf[ctx->recent_cmdu_rx_if_number].if_name,
+			ctx->itf[ctx->recent_cmdu_rx_if_number].trx_config);
+		return -1;
+	}
+
+	return 0;
+}
+
 /**
  *  bridge init
  *
@@ -814,12 +826,12 @@ int process_cmdu_txq(struct p1905_manage
 		} else {
 			/*drop this packet that does not allow to send multicast*/
 			if (!(ctx->itf[i].trx_config & TX_MUL) && !os_memcmp(tlist->dmac, p1905_multicast_address, ETH_ALEN)) {
-				debug(DEBUG_TRACE, "drop send multicast packet on %s\n", ctx->itf[i].if_name);
+				debug(DEBUG_TRACE, "drop tx multicast packet on %s; this intf has no tx multicast cap\n", ctx->itf[i].if_name);
 				continue;
 			}
 
 			if (!(ctx->itf[i].trx_config & TX_UNI) && os_memcmp(tlist->dmac, p1905_multicast_address, ETH_ALEN)) {
-				debug(DEBUG_TRACE, "drop send unicast packet on %s\n", ctx->itf[i].if_name);
+				debug(DEBUG_TRACE, "drop tx unicast packet on %s; this intf has no tx unicast cap\n", ctx->itf[i].if_name);
 				continue;
 			}
 		}
@@ -1579,12 +1591,8 @@ int cmdu_parse(struct p1905_managerd_ctx
 
     eth_hdr = (struct ethhdr*)buf;
 
-	if (!(ctx->itf[ctx->recent_cmdu_rx_if_number].trx_config & RX_UNI) &&
-		os_memcmp(eth_hdr->h_dest, p1905_multicast_address, ETH_ALEN)) {
-			debug(DEBUG_TRACE, "drop rx unicast packet on %s\n",
-				ctx->itf[ctx->recent_cmdu_rx_if_number].if_name);
+	if (cmdu_recv_intf_check(ctx) < 0)
 		return 0;
-	}
 
     /*examine the dst mac address to decide if we need to parse this packet*/
    if(!cmdu_dst_mac_check(ctx, eth_hdr->h_dest))
--- a/src/cmdu_message_parse.c
+++ b/src/cmdu_message_parse.c
@@ -896,11 +896,6 @@ int parse_topology_discovery_message(str
 	for (ifid = 0; ifid < ctx->itf_number; ifid++) {
 		if (!os_memcmp(itf_mac_addr, ctx->itf[ifid].mac_addr, ETH_ALEN)) {
 			if (ctx->itf[ifid].is_wifi_sta) {
-				if (!(ctx->itf[ifid].trx_config & RX_MUL)) {
-					debug(DEBUG_ERROR, "%s link down, drop discovery\n",
-						ctx->itf[ifid].if_name);
-					return -1;
-				}
 				if_sta = 1;
 				break;
 			} else if (ctx->itf[ifid].media_type == IEEE802_3_GROUP) {
--- a/src/cmdu_tlv_parse.c
+++ b/src/cmdu_tlv_parse.c
@@ -129,6 +129,9 @@ int parse_vs_tlv(unsigned char *buf, str
 	unsigned short length = 0, sublen = 0;
 	unsigned char func_type = 0, sub_type = 0;
 	unsigned char suboui[3] = {0x00, 0x0C, 0xE7};
+#ifdef MAP_R2
+	unsigned short parse_len = 0;
+#endif
 
     if ((*temp_buf) == VENDOR_SPECIFIC_TLV_TYPE)
         temp_buf++;
@@ -202,12 +205,28 @@ int parse_vs_tlv(unsigned char *buf, str
 							break;
 
 						num = *temp_buf++;
+						if (num > MAX_NUM_TRANSPARENT_VID) {
+							debug(DEBUG_TRACE, "num(%d) is larger than MAX_NUM_TRANSPARENT_VID\n", num);
+							return -1;
+						}
+
+						parse_len = 9;
+
+						if (length < parse_len) {
+							debug(DEBUG_TRACE, "tlv len (%d) is less than need parse len (%d)\n", length, parse_len);
+							return -1;
+						}
 
 						ctx->map_policy.trans_vlan.num = num;
 						debug(DEBUG_ERROR, "parse transparent vlan number=%d\n", num);
 						for (i = 0; i < num; i++) {
 							ctx->map_policy.trans_vlan.transparent_vids[i] = be_to_host16(*((unsigned short*)temp_buf));
 							temp_buf += 2;
+							parse_len += 2;
+							if (length < parse_len) {
+								debug(DEBUG_TRACE, "tlv len (%d) is less than need parse len (%d)\n", length, parse_len);
+								return -1;
+							}
 							debug(DEBUG_ERROR, "parse transparent vlan id(%d)\n",
 								ctx->map_policy.trans_vlan.transparent_vids[i]);
 						}
--- a/src/multi_ap.c
+++ b/src/multi_ap.c
@@ -13983,3 +13983,23 @@ int check_add_error_code_tlv(struct p190
 	return 0;
 }
 
+void set_trx_config_for_bss(struct p1905_managerd_ctx*ctx, unsigned char map_vendor_extension, unsigned char *bss_mac)
+{
+                unsigned int i = 0;
+
+                for (i = 0; i < ctx->itf_number; i++) {
+                                if (!os_memcmp(ctx->itf[i].mac_addr, bss_mac, ETH_ALEN) &&
+                                                ctx->itf[i].is_wifi_ap) {
+                                                if (!(map_vendor_extension & BIT_BH_BSS))
+                                                                ctx->itf[i].trx_config = 0;
+                                                else
+                                                                ctx->itf[i].trx_config = TX_MUL | RX_MUL | TX_UNI | RX_UNI;
+                                                debug(DEBUG_ERROR, "%s is %s bss; %s all tx rx for this interface\n",
+                                                                ctx->itf[i].if_name,
+                                                                map_vendor_extension & BIT_BH_BSS ? "bh" : "fh",
+                                                                map_vendor_extension & BIT_BH_BSS ? "enable" : "disable"
+                                                );
+                                                return;
+                                }
+                }
+}
--- a/src/wsc_attr_tlv.c
+++ b/src/wsc_attr_tlv.c
@@ -754,10 +754,15 @@ WSC_ATTR_STATUS parse_ssid_attr(
     unsigned char *temp;
     struct p1905_managerd_ctx *ctx_temp = (struct p1905_managerd_ctx *)ctx;
 
+	if (*length > SSID_MAX_LEN) {
+	    debug(DEBUG_ERROR, "length check fail! msg_len(%d) > max_len(32)parse fail\n", *length);
+	    return wsc_attr_error;
+	}
+
 	if(ctx_temp->role == AGENT && ctx_temp->is_in_encrypt_settings) {
 	    temp = pkt;
 
-	    memset(ctx_temp->ap_config_data.Ssid, 0, 32+1);
+	    memset(ctx_temp->ap_config_data.Ssid, 0, SSID_MAX_LEN+1);
 	    memcpy(ctx_temp->ap_config_data.Ssid, temp, (*length));
 	    ctx_temp->get_config_attr_kind |= (1 << HAS_SSID_ATTR);
 	}
@@ -856,6 +861,11 @@ WSC_ATTR_STATUS parse_encrypt_settings_a
 	}
 	memset(dec->CipherText, 0, 512);
 
+	if (*length < 16 || *length > 512 + 16 ) {
+		debug(DEBUG_ERROR, "length check fail! \n");
+		status = wsc_attr_error;
+		goto end;
+	}
     dec->CipherTextLen = (*length) - 16;
     memcpy(dec->CipherText, temp, dec->CipherTextLen);
     if(wifi_utils_success != generate_AES_CBC_decrypt_value(dec))
@@ -1010,7 +1020,10 @@ WSC_ATTR_STATUS parse_network_key_attr(
 	    }
 
 	    temp = pkt;
-
+        if (*length > 64) {
+            debug(DEBUG_ERROR, "length check fail! msg_len(%d) > max_len(64) parse fail\n", *length);
+            return wsc_attr_error;
+        }
         memset(ctx_temp->ap_config_data.WPAKey, 0, 64+1);
         memcpy(ctx_temp->ap_config_data.WPAKey, temp, (*length));
 
@@ -1218,12 +1231,21 @@ WSC_ATTR_STATUS parse_vendor_extension_a
 
 	if (temp_ctx->role == CONTROLLER)
 		return wsc_attr_success;
+	if (attr_len < sizeof(WFA_vendor_ext_attr_id)) {
+		debug(DEBUG_ERROR, " current total len (%d) is less than attr id len (%d)\n", attr_len, (unsigned int)sizeof(WFA_vendor_ext_attr_id));
+		return wsc_attr_success;
+	}
 
 	if (!memcmp(pkt, WFA_vendor_ext_attr_id, sizeof(WFA_vendor_ext_attr_id))) {
 		pkt += 3; /*skip Vendor ID 0x00372A*/
 		attr_len -= 3;
 		while (attr_len) {
 			attr = (struct wfa_subelements_attr *)pkt;
+			if (attr_len < (sizeof(*attr) + attr->attribute_length)) {
+				debug(DEBUG_ERROR, " current total len (%d) is less than attr len (%d)\n", attr_len, (unsigned int)(sizeof(*attr) + attr->attribute_length));
+				break;
+			}
+
 			if(attr->attribute == 0x06) {/*MAP extension attribute*/
 				debug(DEBUG_TRACE, "get Muilt-AP Entension Attibute, value=%02x\n", attr->attribute_value[0]);
 				temp_ctx->ap_config_data.map_vendor_extension = attr->attribute_value[0];
@@ -1240,6 +1262,10 @@ WSC_ATTR_STATUS parse_vendor_extension_a
 		attr_len -= 3;
 		while (attr_len) {
 			attr = (struct wfa_subelements_attr *)pkt;
+			if (attr_len < (sizeof(*attr) + attr->attribute_length)) {
+				debug(DEBUG_ERROR, " current total (%d) is less than attr len (%d)\n", attr_len, (unsigned int)(sizeof(*attr) + attr->attribute_length));
+				break;
+			}
 			if(attr->attribute == 0x00) {/*hidden ssid attribute*/
 				debug(DEBUG_TRACE, "get MTK Hidden Ssid Attibute, value=%02x\n", attr->attribute_value[0]);
 				temp_ctx->ap_config_data.hidden_ssid= attr->attribute_value[0];
