Index: linux-4.4.198/drivers/net/ethernet/mediatek/mtk_eth_soc.c
===================================================================
--- linux-4.4.198.orig/drivers/net/ethernet/mediatek/mtk_eth_soc.c
+++ linux-4.4.198/drivers/net/ethernet/mediatek/mtk_eth_soc.c
@@ -1590,7 +1590,11 @@ static int mtk_rx_alloc(struct mtk_eth *
 		return -ENOMEM;
 
 	for (i = 0; i < rx_dma_size; i++) {
+		if (rx_flag == MTK_RX_FLAGS_HWLRO) {
+		    ring->data[i] = kmalloc(ring->frag_size, GFP_KERNEL);
+			} else {
 		ring->data[i] = netdev_alloc_frag(ring->frag_size);
+				}
 		if (!ring->data[i])
 			return -ENOMEM;
 	}
@@ -1630,7 +1634,7 @@ static int mtk_rx_alloc(struct mtk_eth *
 	return 0;
 }
 
-static void mtk_rx_clean(struct mtk_eth *eth, struct mtk_rx_ring *ring)
+static void mtk_rx_clean(struct mtk_eth *eth, struct mtk_rx_ring *ring, int rx_flag)
 {
 	int i;
 
@@ -1644,8 +1648,12 @@ static void mtk_rx_clean(struct mtk_eth
 					 ring->dma[i].rxd1,
 					 ring->buf_size,
 					 DMA_FROM_DEVICE);
+			if (rx_flag == MTK_RX_FLAGS_HWLRO) {
+			         kfree(ring->data[i]);
+				} else{
 			skb_free_frag(ring->data[i]);
 		}
+		}
 		kfree(ring->data);
 		ring->data = NULL;
 	}
@@ -2000,13 +2008,13 @@ static void mtk_dma_free(struct mtk_eth
 		eth->phy_scratch_ring = 0;
 	}
 	mtk_tx_clean(eth);
-	mtk_rx_clean(eth, &eth->rx_ring[0]);
-	mtk_rx_clean(eth, &eth->rx_ring_qdma);
+	mtk_rx_clean(eth, &eth->rx_ring[0], MTK_RX_FLAGS_QDMA | MTK_RX_FLAGS_NORMAL);
+	mtk_rx_clean(eth, &eth->rx_ring_qdma, MTK_RX_FLAGS_QDMA | MTK_RX_FLAGS_NORMAL);
 
 	if (eth->hwlro) {
 		mtk_hwlro_rx_uninit(eth);
 		for (i = 1; i < MTK_MAX_RX_RING_NUM; i++)
-			mtk_rx_clean(eth, &eth->rx_ring[i]);
+			mtk_rx_clean(eth, &eth->rx_ring[i], MTK_RX_FLAGS_HWLRO);
 	}
 
 	kfree(eth->scratch_head);
Index: linux-4.4.198/include/linux/mm_types.h
===================================================================
--- linux-4.4.198.orig/include/linux/mm_types.h
+++ linux-4.4.198/include/linux/mm_types.h
@@ -240,7 +240,7 @@ struct page_frag {
 #endif
 };
 
-#define PAGE_FRAG_CACHE_MAX_SIZE	__ALIGN_MASK(32768, ~PAGE_MASK)
+#define PAGE_FRAG_CACHE_MAX_SIZE	__ALIGN_MASK(8192, ~PAGE_MASK)
 #define PAGE_FRAG_CACHE_MAX_ORDER	get_order(PAGE_FRAG_CACHE_MAX_SIZE)
 
 struct page_frag_cache {
Index: linux-4.4.198/mm/page_alloc.c
===================================================================
--- linux-4.4.198.orig/mm/page_alloc.c
+++ linux-4.4.198/mm/page_alloc.c
@@ -3419,6 +3419,11 @@ void *__alloc_page_frag(struct page_frag
 	struct page *page;
 	int offset;
 
+        if(fragsz >= PAGE_FRAG_CACHE_MAX_SIZE) {
+		printk("[!!!Error]  __alloc_page_frag frag length is %d \n",fragsz);
+        	show_stack(current, NULL);
+		    return NULL;
+	}
 	if (unlikely(!nc->va)) {
 refill:
 		page = __page_frag_refill(nc, gfp_mask);
